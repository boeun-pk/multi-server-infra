all:
  children:
    web_servers:
      hosts:
        192.168.56.101:
          ansible_become_password: "{{ vault_web01_pass }}"
        192.168.56.103:
          ansible_become_password: "{{ vault_web02_pass }}"
    db_servers:
      hosts:
        192.168.56.102:
          ansible_become_password: "{{ vault_db01_pass }}"
    lb_servers:
      hosts:
        192.168.56.104:
          ansible_connection: local
          ansible_become_password: "{{ vault_lb01_pass }}"

